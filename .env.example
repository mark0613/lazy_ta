# Google API key，支援多組 key 輪調以實現平行處理，使用逗號 (,) 隔開每組 key 即可
GOOGLE_API_KEY=your_gemini_api_key_here,another_gemini_api_key_here

# 編譯器配置
GCC_PATH=gcc
GPP_PATH=g++
COMPILE_TIMEOUT=5

# 測試執行配置
TEST_TIMEOUT=5
MEMORY_LIMIT_MB=256

# LLM 配置
LLM_MODEL=gemini-2.5-flash
